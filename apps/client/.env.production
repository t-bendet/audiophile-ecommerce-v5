# Interim (#208): the client is on Cloudflare while the API is still on Render.
# Returns to /api/v1 when the API moves under the same hostname (#210).
VITE_APP_API_URL=https://audiophile-server-mhie.onrender.com/api/v1
